/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [],
    theme: {
        fontFamily: {
            "source-code-pro": ["Montserrat"]
        }
    },
    plugins: [
        require('tailwind-nord'),
    ],
}

